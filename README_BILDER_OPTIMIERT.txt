FotoJoes V36.2 SEO – optimierte Bilddateien

Die Fototassen-Bilder wurden deutlich verkleinert und behalten ihre Dateinamen.
Dadurch müssen die HTML-Verweise nicht geändert werden.
Die Bilder bleiben als echte Dateien erhalten, damit Google sie als Product-image-URL abrufen kann.
Base64-Einbettung in HTML wurde bewusst NICHT verwendet, da sie die HTML-Datei vergrößert und für Google-Produktbilder ungeeignet ist.
